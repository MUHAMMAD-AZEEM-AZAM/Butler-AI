"""Optional learned motor-policy layer (LeRobot ACT). Nothing here is used by plan().

torch, lerobot and pyarrow are imported lazily inside functions, so this package
imports with only the Stage 3 planner requirements installed.
"""
